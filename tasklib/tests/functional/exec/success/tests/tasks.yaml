- id: test-tests
  type: shell
  role: []
  requires: []
  required_for: []
  parameters:
    cmd: /bin/echo "task start" && /bin/true && /bin/echo "task end"
    timeout: 180
  test_pre:
    cmd: /bin/echo "pre start" && /bin/true && /bin/echo "pre end"
  test_post:
    cmd: /bin/echo "post start" && /bin/true && /bin/echo "post end"
